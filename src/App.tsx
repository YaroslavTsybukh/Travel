import { Header, Gallery } from './components';

import './styles/main.scss';

export const App = () => {
    return (
        <>
            <Header />
            <main>
                <Gallery />
            </main>
        </>
    );
};
